apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  namespace: project-anurag
spec:
  replicas: 2
  selector:
    matchLabels:
      app: project-service
  template:
    metadata:
      labels:
        app: project-service
    spec:
      containers:
      - name: project-service
        image: mrudul/project-service:latest
        ports:
        - containerPort: 4002
