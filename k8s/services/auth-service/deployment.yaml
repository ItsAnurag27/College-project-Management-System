apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: project-anurag
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - name: auth-service
        image: mrudul/auth-service:latest
        ports:
        - containerPort: 4001
